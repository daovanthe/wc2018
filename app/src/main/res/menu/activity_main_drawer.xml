<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_live_score"
            android:icon="@drawable/ic_menu_live_score"
            android:title="Live Scores" />
        <item
            android:id="@+id/nav_matches"
            android:icon="@drawable/ic_menu_matches"
            android:title="Matches" />
        <item
            android:id="@+id/nav_group_score"
            android:icon="@drawable/ic_menu_group_score"
            android:title="Group Score" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
